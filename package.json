{
  "name": "pokescan",
  "version": "9.49.0",
  "private": true,
  "description": "PokeScan server: serves the app and syncs scans/roster to Postgres",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "test": "node server/test.js",
    "test:e2e": "playwright test",
    "test:all": "npm test && npm run test:e2e"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.124.0",
    "@clerk/backend": "^2.33.6",
    "@fastify/static": "^8.1.1",
    "fastify": "^5.3.2",
    "pg": "^8.14.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
